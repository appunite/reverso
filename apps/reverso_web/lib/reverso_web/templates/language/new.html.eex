<h2>New Language</h2>

<%= render "form.html", changeset: @changeset,
                        action: language_path(@conn, :create) %>

<span><%= link "Back", to: language_path(@conn, :index) %></span>
