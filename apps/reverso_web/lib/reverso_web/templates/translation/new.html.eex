<h2>New Translation</h2>

<%= render "form.html", changeset: @changeset,
                        action: translation_path(@conn, :create) %>

<span><%= link "Back", to: translation_path(@conn, :index) %></span>
