# .deliver/config

APP="reverso_web"

BUILD_HOST="coreos2.appunite.net"
BUILD_USER="builds"
BUILD_AT="/home/builds/reverso"

STAGING_HOSTS="coreos2.appunite.net"
STAGING_USER="reverso"
DELIVER_TO="/home/reverso"


pre_erlang_clean_compile() {
  status "Preparing assets with: brunch build and phoenix.digest"
  __sync_remote "
    # runs the commands on the build host
    [ -f ~/.profile ] && source ~/.profile # load profile (optional)

    # fail if any command fails (recommended)
    set -e

    # enter the build directory on the build host (required)
    cd '$BUILD_AT/apps/reverso_web'

    mkdir -p priv/static # required by the phoenix.digest task

    cd assets/

    # installing npm dependencies
    yarn install

    # building brunch
    brunch build --production

    # run your custom task
    APP='$APP' MIX_ENV='$TARGET_MIX_ENV' $MIX_CMD phoenix.digest $SILENCE
  "
}
